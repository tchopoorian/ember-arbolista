import Ember from 'ember';
import layout from '../templates/components/arbolista-node-decorator';

const { set, get, computed } = Ember;

export default Ember.Component.extend({
  layout,
  
  tagName: ''

});
